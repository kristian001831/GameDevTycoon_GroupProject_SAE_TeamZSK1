using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//from lucas class
public class Saving : MonoBehaviour
{/*
    AestheticSystem aest = new AestheticSystem();
    Game game = new Game();

    //OnLoad
    SaveSystem.Load(aest);
    SaveSystem.Load(game);


    //OnQuit
    SaveSystem.Save(aest);

    bool saveSuccessful = SaveSystem.Save(game);

        if (saveSuccessful)
    {
        //Quit
    }
    else
    {
        // give option to save again, cancel quit
   }*/
}
